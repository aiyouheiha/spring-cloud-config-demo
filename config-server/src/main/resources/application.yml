server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aiyouheiha/spring-cloud-config-demo
          search-paths: config-repo
          username: username
          password: password
      label: master
